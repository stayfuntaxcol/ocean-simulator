{
  "name": "ocean-simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:reef-review": "node scripts/build-reef-review.mjs",
    "build:micro-review": "node scripts/build-micro-review.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "three": "0.179.1"
  }
}
